<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="re.pablochrun.com.reopendoors">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/key"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".ihm.LoginMain">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ihm.AdminDoorPasswords" />
        <activity android:name=".ihm.MainScreenGames" />
        <activity android:name=".ihm.DoorOnePass" />
        <activity android:name=".ihm.GameSelectorScreen" />
        <activity android:name=".ihm.DoorTwoPass" />
        <activity android:name=".ihm.DoorPassParent"></activity>
    </application>

</manifest>